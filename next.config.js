/** @type {import('next').NextConfig} */
const nextConfig = {
  // Next.js 13+ uses App Router, API routes are in app/api
  // Body size limit is handled by server configuration
};

module.exports = nextConfig;

